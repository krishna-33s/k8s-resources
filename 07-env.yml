apiVersion: v1
kind: Pod
metadata:
  name: env-demo
  namespace: roboshop
spec:
  containers:
  - name: nginx
    image: nginx:1.14.2
    env:
    - name: project
      value: roboshop
    - name: environment
      value: dev  