apiVersion: v1
kind: Service
metadata:
  name: nginx-loadbalancer
  namespace: roboshop
spec:
  type: LoadBalancer
  selector:
    project: roboshop
    environment: dev
    component: frontend

  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    nodeport: 30715