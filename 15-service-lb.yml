apiVersion: v1
kind: Service
metadata:
  name: mongodb-lb
  namespace: roboshop
spec:
  selector:
    project: roboshop
    environment: dev
    component: frontend

  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 30715